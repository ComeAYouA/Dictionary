<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewModel"
            type="com.lithium.kotlin.dictionary.presentation.dictionary.WordViewModel" />
    </data>

    <com.lithium.kotlin.dictionary.presentation.dictionary.worditem.WordItem
        android:id="@+id/word_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/edit_text_new"
        android:layout_margin="4dp">

        <ImageButton
            android:id="@+id/word_icon"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:src="@drawable/ic_empty_picture"
            android:background="@null"/>

        <EditText
            android:id="@+id/word_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ems="10" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="Удалить" />

    </com.lithium.kotlin.dictionary.presentation.dictionary.worditem.WordItem>

</layout>
